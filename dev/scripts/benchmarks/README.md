# Benchmark Scripts

These scripts are manual performance probes and are intentionally kept outside
`dev/tests/` so pytest does not try to collect them as automated test coverage.

## Files

| File | Purpose |
| --- | --- |
| `plot_2d_network_benchmark.py` | Generate a large synthetic network and time `NetworkVisualizer.plot_2d_network()` |

