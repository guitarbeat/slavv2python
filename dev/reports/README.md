# Reports Moved

Report documents were moved to `docs/reports/`.

Use:

- `docs/reports/README.md` for the status catalog
- `docs/reports/handled/` for handled reports
- `docs/reports/unhandled/` for unhandled reports
